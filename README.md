# Product compartor based on React.
This is simple application(proof of concept) for feature comparison of products(two for now).

### Setup
Install dependencies with 
```
npm install
```

Inorder to start the server, 
```
npm start
```
